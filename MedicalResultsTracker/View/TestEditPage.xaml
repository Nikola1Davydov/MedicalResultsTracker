<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MedicalResultsTracker.View.TestEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:loc="clr-namespace:MedicalResultsTracker.Resources.Strings"
    xmlns:model="clr-namespace:MedicalResultsTracker.Model"
    xmlns:vm="clr-namespace:MedicalResultsTracker.ViewModel"
    Title="{Binding Title}"
    x:DataType="vm:TestEditViewModel">

    <Grid RowDefinitions="*,Auto">

        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="16" Spacing="14">

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="8">
                        <Label Style="{StaticResource Caption}" Text="{loc:Tr Edit_Date}" />
                        <DatePicker Date="{Binding Date}" Format="dd.MM.yyyy" />

                        <Label Style="{StaticResource Caption}" Text="{loc:Tr Edit_Lab}" />
                        <Entry Placeholder="{loc:Tr Edit_LabPlaceholder}" Text="{Binding Laboratory}" />

                        <Label Style="{StaticResource Caption}" Text="{loc:Tr Edit_Note}" />
                        <Entry Placeholder="{loc:Tr Edit_NotePlaceholder}" Text="{Binding Notes}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="8">
                        <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Edit_AddParam}" />
                        <SearchBar Placeholder="{loc:Tr Edit_SearchPlaceholder}" Text="{Binding AnalyteQuery}" />

                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding Suggestions}" Spacing="2">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="model:Analyte">
                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TestEditViewModel}}, Path=AddAnalyteCommand}" CommandParameter="{Binding .}" />
                                        </Grid.GestureRecognizers>
                                        <Label
                                            Grid.Column="0"
                                            Padding="0,8"
                                            Text="{Binding Name}" />
                                        <Label
                                            Grid.Column="1"
                                            Padding="0,8"
                                            Style="{StaticResource Caption}"
                                            Text="{Binding Unit}" />
                                    </Grid>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Button
                                Grid.Column="0"
                                Command="{Binding AddTypedNameCommand}"
                                Text="{loc:Tr Edit_AddTyped}" />
                            <Button
                                Grid.Column="1"
                                Command="{Binding AddRowCommand}"
                                Text="{loc:Tr Edit_AddEmpty}" />
                        </Grid>
                        <Button Command="{Binding CopyFromPreviousCommand}" Text="{loc:Tr Edit_CopyPrevious}" />
                        <Label
                            IsVisible="{Binding HasAssistantHint}"
                            Style="{StaticResource Caption}"
                            Text="{Binding AssistantHint}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="8">
                        <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Edit_PhotoTitle}" />
                        <Label Text="{loc:Tr Edit_PhotoBody}" />
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Button
                                Grid.Column="0"
                                Command="{Binding CopyImportPromptCommand}"
                                Text="{loc:Tr Edit_CopyPrompt}" />
                            <Button
                                Grid.Column="1"
                                Command="{Binding PasteRowsCommand}"
                                Text="{loc:Tr Edit_Paste}" />
                        </Grid>
                        <Label Style="{StaticResource Caption}" Text="{loc:Tr Edit_PhotoNote}" />
                    </VerticalStackLayout>
                </Border>

                <VerticalStackLayout BindableLayout.ItemsSource="{Binding Rows}" Spacing="8">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:ParameterRowViewModel">
                            <Border Style="{StaticResource Card}">
                                <VerticalStackLayout Spacing="6">
                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                        <Entry
                                            Grid.Column="0"
                                            Placeholder="{loc:Tr Edit_RowName}"
                                            Text="{Binding Name}" />
                                        <Button
                                            Grid.Column="1"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TestEditViewModel}}, Path=RemoveRowCommand}"
                                            CommandParameter="{Binding .}"
                                            Text="✕"
                                            WidthRequest="48" />
                                    </Grid>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                                        <Entry
                                            Grid.Column="0"
                                            Keyboard="Numeric"
                                            Placeholder="{loc:Tr Csv_Value}"
                                            Text="{Binding ValueText}" />
                                        <Entry
                                            Grid.Column="1"
                                            Placeholder="{loc:Tr Csv_Unit}"
                                            Text="{Binding Unit}" />
                                    </Grid>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                                        <Entry
                                            Grid.Column="0"
                                            Keyboard="Numeric"
                                            Placeholder="{loc:Tr Edit_RowRefMin}"
                                            Text="{Binding RefMinText}" />
                                        <Entry
                                            Grid.Column="1"
                                            Keyboard="Numeric"
                                            Placeholder="{loc:Tr Edit_RowRefMax}"
                                            Text="{Binding RefMaxText}" />
                                    </Grid>
                                    <Label
                                        Style="{StaticResource Caption}"
                                        Text="{Binding StatusText}"
                                        TextColor="{Binding StatusColor}" />
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>

                <Label
                    Style="{StaticResource Caption}"
                    Text="{loc:Tr Edit_Hint}" />

            </VerticalStackLayout>
        </ScrollView>

        <Grid
            Grid.Row="1"
            Padding="16,8"
            ColumnDefinitions="*,*,Auto"
            ColumnSpacing="10">
            <Button
                Grid.Column="0"
                Command="{Binding SaveCommand}"
                Text="{loc:Tr Common_Save}" />
            <Button
                Grid.Column="1"
                Command="{Binding CancelCommand}"
                Text="{loc:Tr Common_Cancel}" />
            <Button
                Grid.Column="2"
                Command="{Binding DeleteCommand}"
                IsVisible="{Binding IsExisting}"
                Text="🗑" />
        </Grid>

    </Grid>

</ContentPage>
