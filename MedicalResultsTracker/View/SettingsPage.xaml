<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MedicalResultsTracker.View.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:loc="clr-namespace:MedicalResultsTracker.Resources.Strings"
    xmlns:vm="clr-namespace:MedicalResultsTracker.ViewModel"
    Title="{Binding Title}"
    x:DataType="vm:SettingsViewModel">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="6">
                    <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Set_StorageTitle}" />
                    <Label Text="{Binding StorageSummary}" />
                    <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_DbFile}" />
                    <Label Style="{StaticResource Caption}" Text="{Binding DatabasePath}" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Set_CatalogTitle}" />
                    <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_CatalogBody}" />
                    <Button Command="{Binding OpenCatalogCommand}" Text="{loc:Tr Set_CatalogOpen}" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Set_ExportTitle}" />
                    <Button Command="{Binding ExportMatrixCommand}" Text="{loc:Tr Set_ExportMatrix}" />
                    <Button Command="{Binding ExportFlatCommand}" Text="{loc:Tr Set_ExportFlat}" />
                    <Button Command="{Binding ExportBackupCommand}" Text="{loc:Tr Set_ExportBackup}" />
                    <Button Command="{Binding ImportBackupCommand}" Text="{loc:Tr Set_ImportBackup}" />
                    <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_ExportNote}" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Set_AiTitle}" />
                    <Label Text="{loc:Tr Set_AiBody}" />

                    <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_AiManual}" />
                    <Button Command="{Binding ShareForAiCommand}" Text="{loc:Tr Set_AiShare}" />
                    <Button Command="{Binding CopyForAiCommand}" Text="{loc:Tr Set_AiCopy}" />

                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                            <Label Text="{loc:Tr Set_AiScan}" />
                            <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_AiScanNote}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1" IsToggled="{Binding AllowDocumentRecognition}" />
                    </Grid>

                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                            <Label Text="{loc:Tr Set_AiComment}" />
                            <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_AiCommentNote}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1" IsToggled="{Binding AllowResultCommentary}" />
                    </Grid>

                    <Label Style="{StaticResource Caption}" Text="{Binding AssistantSummary}" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Label Style="{StaticResource SectionTitle}" Text="{loc:Tr Set_DangerTitle}" />
                    <Button Command="{Binding DeleteAllDataCommand}" Text="{loc:Tr Set_DeleteAll}" />
                </VerticalStackLayout>
            </Border>

            <Label Style="{StaticResource Caption}" Text="{loc:Tr Set_Disclaimer}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
